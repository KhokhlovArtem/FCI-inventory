# Optional SQL authentication for MSSQL-inventory.ps1.
# Load these values into the process environment before running the script.
# Leave both variables empty to use Windows authentication.
MSSQL_INVENTORY_SQL_USER=
MSSQL_INVENTORY_SQL_PASSWORD=
